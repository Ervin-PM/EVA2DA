# Variables de entorno - TODO List API

Este archivo contiene la configuración necesaria para conectar la aplicación al backend.

## Producción

```env
EXPO_PUBLIC_API_URL=https://todo-list.dobleb.cl
```

## Desarrollo Local (si usas un servidor local)

```env
EXPO_PUBLIC_API_URL=http://localhost:3000
```

## Notas

- El prefijo `EXPO_PUBLIC_` es requerido para que Expo incluya la variable en el bundle
- Las variables se reemplazan en tiempo de compilación
- Cambios en `.env.local` requieren reiniciar `npx expo start`
- Nunca comitees `.env.local` con secretos; es solo local
